/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ['api.vetone.cafesiran.ir', 'api.vet-one.ir', 'trustseal.enamad.ir']
  }
}

module.exports = nextConfig
